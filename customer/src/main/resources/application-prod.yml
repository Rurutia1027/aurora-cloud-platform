server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: ${APP_NAME:customer}

  datasource:
    url: ${DB_URL:jdbc:postgresql://postgres.aurora-prod.svc.cluster.local:5432/customer}
    username: ${DB_USERNAME:admin}
    password: ${DB_PASSWORD:admin}

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  rabbitmq:
    addresses: ${RABBITMQ_ADDR:amqp://rabbitmq.aurora-prod.svc.cluster.local:5672}

  cloud:
    discovery:
      enabled: false
    loadbalancer:
      ribbon:
        enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: INFO
