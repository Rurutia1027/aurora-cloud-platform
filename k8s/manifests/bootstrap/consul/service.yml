apiVersion: v1
kind: Service
metadata:
  name: consul
  namespace: aurora-dev
spec:
  ports:
    - port: 8500
      targetPort: 8500
    - port: 8600
      targetPort: 8600
      protocol: UDP
  selector:
    app: consul