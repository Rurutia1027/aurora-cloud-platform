apiVersion: v1
kind: Service
metadata:
  name: consul
  namespace: aurora-prod
spec:
  selector:
    app: consul
  ports:
    - name: http
      port: 8500
      targetPort: 8500
      protocol: TCP
    - name: dns
      port: 8600
      targetPort: 8600
      protocol: UDP
