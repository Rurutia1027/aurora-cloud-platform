apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init-cm
  namespace: aurora-prod
data:
  01_create_customer_db.sql: |
    CREATE DATABASE IF NOT EXISTS customer;
  02_create_notification_db.sql: |
    CREATE DATABASE IF NOT EXISTS notification;
  03_create_fraud_db.sql: |
    CREATE DATABASE IF NOT EXISTS fraud;
